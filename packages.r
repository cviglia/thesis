# pacchetti

library(vegan)
# ???

library(openxlsx)
# to read the worksheet

library(bipartite)
# to make the network

library(ggplot2)
# to make the plot

library(hrbrthemes)
# for the theme with colours

library(MASS)
# regression analysis

library(networkD3)
# to make sankey diagram

library(dplyr)
# manipulation of sankey
